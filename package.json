{
  "name": "tigerxinsights",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "buildapp": "pnpm install && cd ./packages/db/ && npx prisma generate && cd .. && cd ..",
    "startapp": "cd ./apps/web/ && pnpm run build && pnpm run start"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.3.3",
    "typescript": "5.5.4"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}